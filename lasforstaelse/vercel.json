{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install && cd api && npm install && cd ..",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ],
  "ignoreCommand": "git diff --quiet origin/main $VERCEL_GIT_COMMIT_SHA -- lasforstaelse/ 2>/dev/null && exit 0 || exit 1"
}
