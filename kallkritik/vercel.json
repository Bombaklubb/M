{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "ignoreCommand": "git diff HEAD~1 HEAD -- kallkritik/ 2>/dev/null | grep -q . && exit 1 || exit 0"
}
