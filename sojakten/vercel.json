{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/" }],
  "ignoreCommand": "git diff --quiet origin/main $VERCEL_GIT_COMMIT_SHA -- sojakten/ 2>/dev/null && exit 0 || exit 1"
}
