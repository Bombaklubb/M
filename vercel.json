{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "installCommand": "npm install && cd lasforstaelse && npm install",
  "buildCommand": "npm run build",
  "outputDirectory": "lasforstaelse/dist",
  "ignoreCommand": "if [ \"$VERCEL_ENV\" != \"production\" ]; then exit 0; fi; git diff --quiet HEAD^ HEAD -- lasforstaelse/ kallkritik/ api/ 2>/dev/null && exit 0 || exit 1"
}
