{
  "rewrites": [{ "source": "/(.*)", "destination": "/" }],
  "ignoreCommand": "[ -z \"$VERCEL_GIT_PREVIOUS_SHA\" ] && exit 1; git diff $VERCEL_GIT_PREVIOUS_SHA $VERCEL_GIT_COMMIT_SHA --quiet -- lasforstaelse/ kallkritik/ api/"
}
